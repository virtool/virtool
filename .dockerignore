.idea
data
**/dist
docker
docs
**/node_modules
# Rust build output. Sending packages/pathoscope-core/target into the build
# context would be gigabytes and would invalidate the context on every local
# cargo run; the image builds the crate from source in its own stage.
**/target
.git
.github
