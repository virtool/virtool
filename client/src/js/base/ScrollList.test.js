import { ScrollList } from "./ScrollList";

decsribe("<ScrollList />", () => {

    it("renders correctly", () => {

    });

    it("adds event listener on componentDidMount", () => {

    });

    it("removes event listener on componentWillUnmount", () => {

    });

    it("onScroll()", () => {

    });

    it("loadMore()", () => {
        
    });
});
