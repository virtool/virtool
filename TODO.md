# TODO

- [x] Share GenBank validation between new-OTU and existing-OTU flows, and enforce a species-level taxonomy match when adding an isolate. Reject mixed organisms and mismatched species, handle missing taxonomy explicitly, and validate again at the versioned save boundary using verifiable accession/taxon provenance rather than trusting the preview. Reuse record-to-segment matching where practical and test matching strains, mismatches, missing taxonomy, and saves that bypass or outlive the preview.
- [x] Capitalize isolate type labels automatically when shown to users (for example, store `isolate` in the database and display `Isolate`). Keep the stored values lowercase.
- [x] Consolidate v2 Reference, OTU, and isolate validation into clear shared boundaries. Reuse one transactional guard for writable local OTUs, deleted state, and expected versions; apply the same isolate plan rules on create as on initial OTU creation (unique sequence IDs, valid and non-duplicate segments, required segments, and length tolerance). Keep request shape in shared contracts, authorization at the server boundary, state-dependent rules in the data layer, and database constraints as the final integrity guard. Test direct writes as well as preview-driven writes.
