import type { DataMigrationRegistry } from "./define";

/** Historical implementations retained for databases upgrading through their paired SQL. */
export const DATA_MIGRATIONS: DataMigrationRegistry = {};
