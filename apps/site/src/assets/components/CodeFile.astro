---
import { FileCode } from "@lucide/astro";

const { name } = Astro.props;
---

<div class="bg-slate-300 rounded-md">
  <h4 class="flex items-center gap-2 px-3 pt-2">
    <FileCode size={16} />{name}
  </h4>
  <slot />
</div>
